// app/pmf/page.tsx
// PMF Score Dashboard — segment 別スコアとフェーズ判定を俯瞰する
// EN: PMF Score | JP: PMFスコア

import PMFScreen from "@/ui/screens/PMFScreen";

export default function PMFPage() {
  return <PMFScreen />;
}
