// src/chasm/config/chasmWeights.ts
// Chasm Breakthrough Phase Score 重み定数 — ここを変更するだけで全スコア計算に反映される
// 因果ループ: 重みはエビデンスの「重要度」を表し、スコア計算の入力となる

export const CHASM_WEIGHTS = {
  // ── 次元重み (Dimension weights) ─────────────────────────────────────────
  /** 市場支配次元の重み */
  wD: 0.40,
  /** リファレンス次元の重み */
  wR: 0.35,
  /** 拡張次元の重み */
  wX: 0.25,

  // ── 支配サブ重み (Dominance sub-weights) ─────────────────────────────────
  /** 商談勝率 */
  w_wr: 0.35,
  /** 成約速度（逆数: 速いほど高スコア） */
  w_dv: 0.25,
  /** 更新率 */
  w_rr: 0.25,
  /** マルチスレッディングスコア */
  w_mt: 0.15,

  // ── リファレンスサブ重み (Reference sub-weights) ──────────────────────────
  /** リファレンス数 */
  w_rc: 0.30,
  /** リファレンス強度 */
  w_rs: 0.30,
  /** Before/After 明確度 */
  w_ba: 0.25,
  /** 業界導入密度 */
  w_rd: 0.15,

  // ── 拡張サブ重み (Expansion sub-weights) ─────────────────────────────────
  /** 隣接セグメント適合度 */
  w_asf: 0.30,
  /** 導入障壁（逆数: 低いほど高スコア） */
  w_ab:  0.25,
  /** 価格感度（逆数: 低いほど高スコア） */
  w_ps:  0.20,
  /** 拡張成功率 */
  w_esr: 0.25,
} as const;
