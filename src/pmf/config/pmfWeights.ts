// src/pmf/config/pmfWeights.ts
// PMF スコア重み定数 — ここを変更するだけで全スコア計算に反映される
// 因果ループ: 重みはエビデンスの「重要度」を表し、スコア計算の入力となる

export const PMF_WEIGHTS = {
  // ── 次元重み (Dimension weights) ─────────────────────────────────────────
  /** 行動次元の重み */
  wB: 0.40,
  /** 感情次元の重み */
  wE: 0.35,
  /** 経済次元の重み */
  wC: 0.25,

  // ── 行動サブ重み (Behavioral sub-weights) ────────────────────────────────
  /** 継続率 */
  w_r:   0.40,
  /** 行動変容スコア */
  w_bc:  0.25,
  /** 価値実感時間 */
  w_ttv: 0.20,
  /** セグメント支配率 */
  w_sd:  0.15,

  // ── 感情サブ重み (Emotional sub-weights) ─────────────────────────────────
  /** Sean Ellis VD比率 */
  w_se: 0.50,
  /** Value Moment 頻度 */
  w_vm: 0.30,
  /** 感情的依存度 */
  w_ed: 0.20,

  // ── 経済サブ重み (Economic sub-weights) ──────────────────────────────────
  /** 支払意欲 */
  w_wtp:   0.35,
  /** LTV */
  w_ltv:   0.35,
  /** CAC（負の寄与） */
  w_cac:   0.15,
  /** 解約感度（負の寄与） */
  w_churn: 0.15,
} as const;
